# find-work 经验借鉴清单（job-helper 设计输入）

日期：2026-08-04
状态：存档参考，非权威规范
来源：find-work 项目调研（2026-08-04 资产盘点）

> 本文档是 job-helper 后续各设计阶段（Agent 架构、数据结构、API、功能设计）的**经验输入**，不是规范、不是实现依据。业务代码零迁移，仅借鉴下述经验。

## 1. 领域设计经验（价值最高）

| # | 经验 | 说明 | 适用阶段 |
|---|---|---|---|
| 1 | 岗位匹配三段式流水线 | 理解岗位要求 → 逐条要求 × 简历证据匹配矩阵 → 投递建议；三段各自独立校验、独立 repair 重试 | Agent 工具设计 |
| 2 | LLM 输出容错哲学 | **校验失败重试修复，绝不静默接受坏数据**（repair 模式）；每段输出有独立 schema 与修复任务 | Agent 工具设计 |
| 3 | 中英枚举别名归一化 | 约 60 条映射（"高"→highly-matched），跨字段一致性校验（requirementId 引用、requiresUserInput 必须配 userInputNeeded），保证跨模型输出稳定 | 契约设计 |
| 4 | 定点替换建议 + 用户逐条审批 | 专属简历生成"定点改写建议"（非整篇重写），每条 sourceText 唯一性校验，用户逐条批准 + 强制事实补充 + 真实性确认 | 功能设计 |
| 5 | 上下文哈希防串扰 | 简历/岗位描述/匹配上下文哈希，检测过期（stale），防止产物与上下文不一致 | 功能设计 |
| 6 | 渠道发现"本地规则护栏" | 域名黑名单（招聘平台/ATS）+ 邮箱/URL 正则 → 本地规则覆写 LLM 结果；严禁 LLM 臆造事实，只给核验动作 | 功能设计 |
| 7 | 隐私边界 | 简历原文可按需剥离后再进 LLM 上下文（resume-text 证据源受控）；本地应用仍值得保留的数据边界意识 | 架构设计 |
| 8 | LLM 产物运行时校验适配 | LLM 输出 → 运行时校验/归一化 → 轮询终态 → 版本化展示的成熟模式 | 前端设计 |

## 2. 工程教训（避免重蹈覆辙）

| # | 教训 | 应对 |
|---|---|---|
| 1 | 测试体系过度膨胀（覆盖率门槛/变异测试/测试资产矩阵/测试治理脚本）导致推进迟缓 | 功能优先，测试仅覆盖核心纯逻辑；已写入初始化设计决策 |
| 2 | 手写本该用库的标准件（JWT、配置解析、JSON schema 校验器、LLM 客户端） | 新项目成熟库优先：AI SDK / zod / Drizzle / shadcn |
| 3 | 规范体系膨胀（91 份 spec，部分失效未淘汰） | 规范自治规范：跨 3 处重复才立规范、失效必归档 |

## 3. find-work 中值得关注的实现细节（供设计时参考）

- 手写 JSON schema 校验器模式（校验 + 归一化 + 枚举别名）：新项目用 zod 替代，但"校验失败分类 + 修复重试"的行为设计可参考
- 队列启用/未启用的进程内降级模式：新项目无队列，但"能力不可用时的降级路径"设计思路可参考
- 乐观锁版本号 + 修订历史：协作编辑类功能可参考
- 每日工作台/工作流规则聚合：纯规则函数设计，对话形态下"推荐行动"的生成思路可参考
- 错误契约 `{code, message, details}` 与 422 字段展开：API 错误格式设计可参考（若后续有 API 边界）

## 3.1 job-helper 第 1 期实测经验（2026-08-05 增补，来自真实 LLM 验证）

- **AI SDK v7 + OpenAI 兼容 provider 的结构化输出兼容性**：当 provider 不支持 `structuredOutputs`（如 deepseek）时，AI SDK 降级为只发 `response_format: json_object`，**zod schema 不会进入提示词**——模型会自创结构导致校验失败。对策：输出契约的完整示例（字段名/枚举值/示例值）必须写进系统提示词。所有领域工具的 prompt 必须遵守此条。
- **AI SDK v7 单请求单步**：streamText 无 maxSteps，一次请求只完成一个模型步骤；存在返回值依赖链的工具（importResume → analyzeResume）需要多轮消息完成，或由前端自动续问。设计依赖链工具时注意此行为。
- **generateObject 的 system 提示**：v7 用 `instructions` 选项传系统提示，塞进 messages 会被 OpenAI 兼容端点拒绝。


## 4. 使用方式

- 各设计阶段（前后端设计、数据结构、API、功能）开始时，先阅读本文档对应条目，再产出设计
- 经验条目被设计采纳后，在设计文档中引用本文档条目号（如"采纳经验 #2"）
- 本文档不随项目演进自动更新；内容过时由设计文档取代，可归档
